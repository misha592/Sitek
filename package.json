{
  "name": "sitek",
  "version": "1.0.0",
  "private": true,
  "description": "Static bio card page with Discord (Lanyard) status and webhook message form",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.8",
    "vitest": "2.1.8"
  }
}
